# BookAI MVP (100% Free)

Receipt upload -> OCR -> extract vendor/date/amount -> categorize -> store -> browse/filter -> export CSV + receipt ZIP.

## 1) Setup (Python)
```bash
python -m venv .venv
source .venv/bin/activate  # Mac/Linux
# .venv\Scripts\activate   # Windows PowerShell

pip install -r requirements.txt

